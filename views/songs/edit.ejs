<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit song details</title>

  <!-- Stylesheets -->
  <link rel="stylesheet" href="/styles/main.css">
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400&display=swap" rel="stylesheet">
  
  
  <!-- Scripts -->
  <script src="/scripts/app.js" defer></script>

</head>
<body>
    <header>
        <nav>
            <a class="navbar-links" href="/">Home</a> 
            <a class="navbar-links" href="/playlists">All Playlists</a>
            <a class="navbar-links" href="/playlists/new">Create a Playlist</a>
            <a class="navbar-links" href="/songs/new">Add a Song</a> 
        </nav>
        <h3>MuzickOnly</h3>
    </header>

    <main>
    
        <div class="form-container"> 
            <h1>Edit Song</h1>
            <form action="/songs/<%= song._id %>?_method=PUT" method="POST">
                <p>
                <input type="text" name="title" value ="<%= song.title %>" required >
                </p>
                <p>
                <input type="text" name="artist" value ="<%= song.artist %>" required >
                </p>

                <input class="submit-btn" type="submit" value="Update Song">
            </form>
        </div>
    </main>
</body>
</html>